# Core dependencies for this repo (embedding extraction, CellViT<->Xenium matching,
# classifier / DeepCell training), pinned to the versions verified in the `cellViT`
# conda env this codebase was developed and run in (Python 3.10). This is a scoped
# list -- only packages actually imported by src/python/ -- not a full `conda list`
# dump of that (shared, multi-project) environment.

# Config / experiment tracking
hydra-core==1.3.2
omegaconf==2.3.0
mlflow==3.6.0

# Data / scientific stack
numpy==1.26.4
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
h5py==3.14.0
pyyaml==6.0.3
tqdm==4.67.1

# Imaging / whole-slide-image I/O. openslide-bin bundles the native OpenSlide library
# itself, so no separate system package (apt/brew/conda) should be needed.
pillow==10.4.0
openslide-python==1.4.3
openslide-bin==4.0.0.8
matplotlib==3.10.8

# Spatial / geometry (CellViT<->Xenium matching, src/python/ot/)
geopandas==1.1.1
shapely==2.0.5

# Single-cell / spatial transcriptomics (deepspot_training)
anndata==0.11.4
scanpy==1.11.4

# Deep learning
torch==2.9.1
torchvision==0.24.1
timm==1.0.22
lightning==2.6.5

# Foundation-model / pathology-specific packages. If `pip install <name>` doesn't
# resolve to the intended package for you, install from source instead:
#   cellvit  -- CellViT-SAM / CellViT-HIPT inference package (extract_embeddings/
#               inference_providers/cellvit_inference_provider.py)
#   conch    -- MahmoodLab CONCH vision-language pathology encoder, fall back to
#               `pip install git+https://github.com/mahmoodlab/CONCH.git`
#   deepspot -- DeepSpot's DeepCell model + dataloader (deepspot_training/ only),
#               fall back to `pip install git+https://github.com/ratschlab/DeepSpot.git`
cellvit==1.0.9
conch==0.1.0
deepspot==1.0.0
